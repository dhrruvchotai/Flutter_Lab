import 'package:flutter/material.dart';

class myCRUD_Form extends StatelessWidget {
  const myCRUD_Form({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('CRUD Using Form'),backgroundColor: Colors.blue,),

    );
  }
}
